# AgentMesh 交接总结（2026-08-29）

> 配套文档：`使用说明书.md`（使用者/修复者上手）、`real_test.md`（17 轮真链路验收台账）、`PROBLEMS.md`（64 条问题台账）、`AgentMesh-决策记录与待办清单.md`（决策与待办）。
> 当前测试基线：**396/396 全绿**，typecheck 干净，dist 已构建。

---

## 一、项目现状

**AgentMesh** 把各家 coding agent 的 CLI（opencode/codex/claude/antigravity）包装成统一 MCP 工具，让任何能当 MCP 客户端的引擎（ZCode 桌面会话）作为"组长"调度组员。组长出大脑，CLI 出手，AgentMesh 出纪律：派发有协议、失败有语义、交接有账本、越界有闸门、出事能回滚。

**已完成并经真链路验证**（17 轮，台账在 real_test.md）：

- OPTIMIZATION_PLAN P1-P5 全部落地：可靠派发（幂等/重试/熔断/看门狗）、Token 计量与压缩（usage 入库/compact_context/artifact 溢出/注入审计 sidecar）、分 vendor 安全基线、强弱调度（元数据路由表+升级链提示）、无人值守闭环（maxReworkRounds 返工/预算闸门/孤儿回收）
- 复杂任务实战：ecom-lab 十假设（双 worktree 并行、真实合并冲突按纪律派发）、可视化面板（ui 子命令）、双通道协同（opencode MCP + Trae 人腿中继）

**本轮（8-29）新增能力**（全部有测试，见下"提交状态"）：

| 能力                           | 说明                                                                                                                                         |
| ------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------- |
| 回滚锚点 + `rollback_task`     | worker 委派前自动 `git stash create` 锚定；出事一键恢复；回滚前再存快照防误回滚                                                              |
| 反作弊证据 `testFilesModified` | worker 改测试文件自动取证并要求评审逐条正当化（防"改断言让测试变绿"）                                                                        |
| 安全扫描 `outputSafety.ts`     | 凭据泄漏（SECURITY:）+ 破坏性命令模式（SAFETY:）警告，delegate/continue 双流程接入                                                           |
| 模型目录校验                   | opencode 指定模型先对照 `opencode models` 目录，ID 错误 spawn 前拒绝并给出最接近建议（零配额消耗）                                           |
| `VENDOR_QUOTA` 错误码          | "预扣费失败/insufficient_user_quota" 归类为供应商额度问题，不再误报"模型坏了"                                                                |
| 后台可恢复                     | kill -9 后 poll 返回 `interrupted`（死信机制）+ 输出文件预创建 + checkpoint 原子写                                                           |
| `review_changes` background    | 评审支持后台（三轮被宿主 30s 掐断的教训）                                                                                                    |
| 存储修复                       | 会话上限失效（淘汰被回灌抵消）、usage 被读盘剥离（预算闸门跨进程失明）两 bug 已修                                                            |
| 可视化面板 `agentmesh ui`      | 四栏布局：任务与成员 / 实时终端（JSONL 译成人话）+ 时间线（组长提示词可展开）/ 检查结论 + Token 消耗（按项目分组、未计量标注）+ 手动操作占位 |

## 二、当前已知问题（未修）

| #   | 问题                                                                                           | 状态/建议                                                                                                          |
| --- | ---------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ |
| 1   | **工作区未提交**：r15→r18 的修复+面板（15+ 文件）都在工作区未 commit（r16 只提交了面板文件集） | **push 前必须先 commit**，建议分两笔：修复笔（core/adapters/tests）+ 文档笔（md 台账）。396 测试是验证载体         |
| 2   | `interrupted` 载荷、continue 路径预算闸门、`review_changes background`、模型目录校验           | 代码已落地，**真链路复验未做**（第十八轮首批事项）                                                                 |
| 3   | 面板数据源由 `AGENTMESH_SESSIONS_FILE` 决定，裸跑 `agentmesh ui` 会读到 C 盘旧库（一堆 0）     | 底部状态栏有提示；长期可做 ui 启动时的家目录显式参数                                                               |
| 4   | 组长自身 token 消耗面板无法计量                                                                | 架构性限制（组长消耗在 ZCode 客户端内部）；面板已用占位文案说明，严禁伪造数字                                      |
| 5   | 免费档长输出不可靠（60k 三连败：超时/45k 封顶/504）                                            | artifact-spill 验收在免费档不可达；>50k 场景需换档或分片                                                           |
| 6   | prompt-only 通道的注入/破坏性指令风险仍在（H5/H9 实测被攻破）                                  | 扫描警告已上线（能发现），真正拦截需 enforced 沙箱通道；**给免费 worker 的任务文本不得含真实凭据与破坏性命令示例** |

## 三、收获与教训（写代码的人必读）

1. **信任模型是地基**：永不信任 worker 的文字自述，只信工作树 diff + 测试 + 评审。这让不可靠通道（免费模型、GUI 中继）也能安全接入——验收证据在仓库里，不在汇报里。
2. **"声明支持≠实际可用"**：能力矩阵三层缺口逐轮暴露——schema 支持（r9 静默丢弃）→ 账户层拒绝（r10 的 400）→ 余额为零（r16 的 wapq 403）。接入任何 vendor 前先验证 headless 通道在账号层真实可用。
3. **zod schema 是数据契约的隐形杀手**：接口里有而 schema 里没有的字段，会在磁盘重读时被静默剥离（usage 失明 bug 藏了三周）。新增证据字段必须三处同步：TS 接口 + zod schema + 写入点。
4. **异常路径的证据留存优先级必须高于正常路径**：r15 发现 SIGKILL 场景"彻底无痕"，修复后又发现死信语义误伤已完成任务（r16 问题 2）——每一次"优雅降级"都要问一句：崩溃时这条路径留下什么？
5. **多写入者必须串行**：r16 面板开发与 r15 修复同时改 runner.ts，typecheck 出现两个来源的交叠错误。纪律：同一文件集同一时间只有一个会话在写。
6. **界面给 人 看，术语必须翻译**：`testFilesModified`→"改了测试文件（评审重点核对）"、`interrupted/死信`→"中途被打断"。术语可进悬停提示，不进正文。
7. **职责边界决定多模型系统的存在价值**：第十七轮组长消耗 6.83M tokens 的根因是组长亲自干检查员的活。已固化节流纪律（AGENTS.md 2.5 节）：组长只裁决，检查派给免费组员，大文件不进组长上下文，单轮组长消耗目标 ≤3M。
8. **测试抓语义错误的价值实证**：回滚测试抓到"head 路径会删暂存新文件"的文档错误；需求闸门 + 基线快照让 Trae 的改动零越界。测试不是验证代码，是验证理解。

## 四、给下一轮（第十八轮）的备忘

- 复验清单：interrupted 载荷（P-R15-1 接线后真链路）、continue 路径预算闸门（P-R15-2）、review background、模型目录校验拒绝路径、回滚端到端
- 待做：`get_agent_stats` 数据回路（路由优化的数据源）、面板按任务归档的分组键可配置、长工具 background 化推广
- 提交后第一件事：在干净 clone 上跑 `npm ci && npm test`，确认 396 基线可复现
