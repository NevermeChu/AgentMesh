# AgentMesh 使用说明书

> 面向两类人：**使用者**（想让主模型调度组员干活的）和**修复者**（接手改代码的）。修复者先读《交接总结-2026-08-29.md》。

---

## 第一部分：这是什么

AgentMesh 把各家 coding agent 的 CLI（opencode/codex/claude/antigravity）包装成统一的 MCP 工具。你的主力 AI（如 ZCode 桌面会话，下称"组长"）通过 MCP 调用这些工具，把任务派给其他模型（"组员"）执行，自己负责拆解、验收、返工调度。

```
你 ──对话──> 组长（ZCode 会话，GLM-5.3-flash 等）
                  │ MCP 工具（delegate_task / review_changes / rollback_task …）
                  ▼
            agentmesh serve（桥接层）
                  │ 每次委派 spawn 一个独立 CLI 进程
                  ├── opencode run（免费模型：mimo/nemotron/hy3）
                  ├── codex exec（强模型，带 OS 沙箱）
                  └── claude -p（deny 规则兜底）
```

**核心原则**：组长负责思考和裁决，不亲自检查；组员产出以仓库 diff 和测试结果为准，不以任何模型的口头汇报为准。

---

## 第二部分：使用者上手

### 2.1 前置条件

1. **安装各 vendor CLI 并完成登录**（用哪个装哪个）：opencode（免费模型池见 2.5）、codex、claude；
2. **Node.js ≥ 22.13**；
3. 在主仓库构建：`npm ci && npm run build && npm test`（应全绿）；
4. **把 agentmesh 注册为组长客户端的 MCP server**。以 ZCode 为例，在 `~/.zcode/cli/config.json` 加：

```json
{
  "mcp": {
    "servers": {
      "agentmesh": {
        "type": "stdio",
        "command": "node",
        "args": ["<agentmesh 绝对路径>/dist/cli/index.js", "serve"],
        "env": {
          "OPENCODE_BIN": "<opencode.exe 绝对路径>",
          "AGENTMESH_SESSIONS_FILE": "<数据目录>/sessions.json"
        }
      }
    }
  }
}
```

> ⚠️ schema 严格校验：未知键会导致整个 server 被静默丢弃。改完重启客户端，到 Settings→MCP 确认状态为 connected。

### 2.2 项目开工（三步）

1. 新开组长会话（工作区选项目根目录）；
2. **只说目标和验收标准**，不用给步骤。说得模糊没关系——组长被纪律约束会先复述理解、最多问 3 个问题、你确认后才开工（需求闸门）；
3. 确认后它会写 `ORCHESTRATION.md`（项目宪法）并开始拆解派发。

### 2.3 项目内的角色配置（可选但推荐）

项目根目录 `.agentmesh/config.json` 定义角色和三档路由：

```json
{
  "version": 1,
  "roles": { "worker": "opencode", "reviewer": "opencode", "tester": "opencode" },
  "agents": {
    "opencode": {
      "tier": "medium",
      "costLevel": 1,
      "strengths": ["功能实现", "测试编写"],
      "candidates": ["claude"]
    }
  }
}
```

派发时用 `model` 参数钉住具体免费模型：`opencode/mimo-v2.5-free`（easy）、`opencode/nemotron-3.5-lightning-free`（mid）、`opencode/nemotron-3-ultra-free`（hard）。**模型 ID 必须完整**——桥接层会对照目录校验，错了会在消耗任何配额前拒绝并给出建议。

### 2.4 可视化面板

```bash
cd <agentmesh 仓库>
set AGENTMESH_SESSIONS_FILE=<数据目录>/sessions.json   # Windows；决定读哪个数据目录
node dist/cli/index.js ui --port 7788
```

浏览器开 `http://127.0.0.1:7788`。四栏：左侧任务与成员（含**任务归属**和模型 ID）、中栏 worker 实时执行过程（已翻译成人话）+ 轮次时间线（组长完整提示词可展开）+ 检查结论、右栏 Token 消耗（按项目分组折叠）与手动操作说明。

常见困惑：

- **看到一堆 0 tokens** → 你读的是旧数据目录。检查底部状态栏的"数据来源"路径，用上面的环境变量指向正确目录后重启 ui 进程；
- **刷新了没变化** → 你看的 ui 进程是旧构建启动的。杀掉进程重新 `node dist/cli/index.js ui`（新构建必须重启进程）；
- **某会话显示"未计量"** → 它早于计量功能，不是 0 消耗。

### 2.5 Trae 人腿中继（无 CLI 的客户端也能当组员）

1. 组长产出 TRAETASK 块（自足任务书 + 复杂度标签 easy/mid/high）+ 为 Trae 建专属 worktree 或指定禁改范围；
2. **你**：粘贴给 Trae（模型自选，分级参考见简报附录 C）→ 点掉确认弹窗 → 回来说"完成"（Trae 的总结不用带）；
3. **组长**：只看 worktree 的 diff 和测试验收——Trae 的文字自述一律不作为 PASS 依据；
4. 返工最多一轮人腿，再不行转 CLI 组员。

### 2.6 使用红线（从 17 轮实战换来的）

- 给免费/prompt-only 组员的任务文本里**不得出现真实凭据或破坏性命令示例**（实测会被照做，H5/H9）；
- 长任务一律让组长用 `background:true` 派发（同步调用会被客户端 30s 掐断）；
- 结果里的 `SECURITY:`/`SAFETY:` 警告未核实前不接受 PASS；
- "done" 的定义必须可判定（测试通过/文件存在），"好用""美观"无效。

---

## 第三部分：修复者指南

### 3.1 仓库与验证链

```
npm ci            # 按 lockfile 装依赖
npm run typecheck # tsc --noEmit
npm test          # vitest 全量（当前基线 396，任何修改不得低于既有数）
npm run build     # tsup 构建 dist（ui 的 panel.html 由 onSuccess 钩子拷贝）
```

提交前四步全绿是硬门禁。**zod schema 是隐形契约**：给 `SessionExecutionEvidence`/`SessionHistoryEntry`/`AgentResult` 加字段必须三处同步（core/types.ts 接口、core/session.ts schema、写入点），漏一处会在磁盘重读时被静默剥离。

### 3.2 目录地图

| 路径                                                 | 内容                                                           |
| ---------------------------------------------------- | -------------------------------------------------------------- |
| `src/agents/*.ts`                                    | 各 vendor 适配器（buildCliArgs/runViaCli/解析器）              |
| `src/core/runner.ts`                                 | 编排核心：delegateTask/continueTask/reviewChanges/rollbackTask |
| `src/core/session.ts`                                | 会话存储（zod schema/文件锁/LRU cap）                          |
| `src/core/background.ts`                             | 后台任务注册表（死信/poll）                                    |
| `src/core/{outputSafety,modelCatalog,resilience}.ts` | 安全扫描/模型目录校验/错误归类与重试                           |
| `src/ui/*`                                           | 可视化面板（data 数据层/api 路由/server/server 静态页）        |
| `tests/`                                             | 396 例（L1 单测 + L2 假 CLI 集成）；fixtures 在 tests/fixtures |
| `real_test.md`                                       | 17 轮真链路验收台账——改代码前先读最近的轮次                    |

### 3.3 修复规范（从 17 轮踩坑换来的）

1. **基线先行**：改代码前全量测试跑一遍记录数字；没有基线的"提升"无法证明；
2. **证据如实**：采不到的指标写 undefined 不填 0；失败如实记录原因码，禁止粉饰成功；
3. **新增证据字段三处同步**（见 3.1）；
4. **报错必须机器可读**：新失败形态归入错误码（resilience.ts 的 pattern 表），附 actionable hint；
5. **真实链路验证**：单测绿 ≠ 修复完成，按 real_test.md 的驱动模式（stdio + 真 vendor，配额封顶）至少跑一轮；
6. **台账更新**：每个问题进 PROBLEMS.md（根因/修复/验证），每轮测试追加 real_test.md；
7. **同文件集串行**：确认没有其他会话在同一仓库写代码再动手（r16 实测过并发撞车）。

### 3.4 常见故障速查

| 症状                                | 原因                                      | 处置                                                       |
| ----------------------------------- | ----------------------------------------- | ---------------------------------------------------------- |
| MCP server 消失/工具不见            | config.json schema 严格，非法键整文件被弃 | 校验 JSON；只保留已知键                                    |
| opencode 报 "not a recognized shim" | PATH 命中坏 shim                          | 用 `OPENCODE_BIN` 绝对路径直指真身 exe                     |
| 委派 30s 被掐                       | 宿主同步 MCP 超时                         | `background:true` + poll_task；SDK 侧 timeout 设 1_800_000 |
| codex 沙箱跑测试报 spawn EPERM      | 沙箱子进程策略                            | `NODE_OPTIONS=--test-isolation=none` 或 glob 形式测试命令  |
| 报"预扣费失败"                      | 调到无余额的自定义 provider               | 钉免费池模型 ID（见 2.3）                                  |
| worker 输出 JSONL 看不懂            | 正常，是 vendor 事件流                    | 面板已翻译；或看 poll_task 增量                            |
| 想撤销 worker 的改动                | 回滚锚点已自动创建                        | 组长调 `rollback_task(sessionId)`                          |

### 3.5 数据与状态

- 会话台账：`$AGENTMESH_HOME/sessions.json`（默认 `~/.agentmesh/`；可用 `AGENTMESH_SESSIONS_FILE` 重定向整个 home）；
- 注入审计：`contexts/<sessionId>/<turn>.txt`（SHA-256 逐字节可复核）；
- 长输出：`artifacts/<sessionId>/turn-n.txt`；后台任务：`tasks/registry.jsonl` + `.output` + `.result.json`；
- 回滚锚点：会话 `metadata.rollbackAnchor`（stash SHA），误回滚可用 `metadata.preRollbackStashSha` 找回。

---

## 附：已知边界（如实告知，不承诺）

- zcode CLI 桌面外不可编程接入（start-plan 端点强制 captcha，P-064 封存）；
- prompt-only 通道无运行时拦截，注入/破坏性指令只有警告没有阻断；
- 免费档模型跑不出 >50k 字符的长输出（artifact 溢出功能在免费档不可达）；
- 组长自身 token 消耗在客户端内部，面板不显示（显示为说明文案）；
- SIGKILL 场景无 checkpoint 可抢救（只有注册死信 + 已写输出）。
